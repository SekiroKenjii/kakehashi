// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: __PROTO_PACKAGE__/authz/v1/authz.proto

// Authorization: who may do what, and to which rows.
//
// Textbook RBAC — accounts hold roles, roles hold permissions — with one thing that is not
// textbook: every grant carries a scope. A permission and the rows it reaches are one decision, so
// they travel together here rather than as two messages a client has to correlate.
package authzv1connect

import (
	v1 "__GO_MODULE__/server/internal/gen/__PROTO_PACKAGE__/authz/v1"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuthzServiceName is the fully-qualified name of the AuthzService service.
	AuthzServiceName = "__PROTO_PACKAGE__.authz.v1.AuthzService"
	// AuthzAdminServiceName is the fully-qualified name of the AuthzAdminService service.
	AuthzAdminServiceName = "__PROTO_PACKAGE__.authz.v1.AuthzAdminService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuthzServiceListMyGrantsProcedure is the fully-qualified name of the AuthzService's ListMyGrants
	// RPC.
	AuthzServiceListMyGrantsProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzService/ListMyGrants"
	// AuthzAdminServiceListRolesProcedure is the fully-qualified name of the AuthzAdminService's
	// ListRoles RPC.
	AuthzAdminServiceListRolesProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/ListRoles"
	// AuthzAdminServiceListPermissionsProcedure is the fully-qualified name of the AuthzAdminService's
	// ListPermissions RPC.
	AuthzAdminServiceListPermissionsProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/ListPermissions"
	// AuthzAdminServiceGetRoleGrantsProcedure is the fully-qualified name of the AuthzAdminService's
	// GetRoleGrants RPC.
	AuthzAdminServiceGetRoleGrantsProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/GetRoleGrants"
	// AuthzAdminServiceSaveRoleGrantsProcedure is the fully-qualified name of the AuthzAdminService's
	// SaveRoleGrants RPC.
	AuthzAdminServiceSaveRoleGrantsProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/SaveRoleGrants"
	// AuthzAdminServiceCreateRoleProcedure is the fully-qualified name of the AuthzAdminService's
	// CreateRole RPC.
	AuthzAdminServiceCreateRoleProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/CreateRole"
	// AuthzAdminServiceUpdateRoleProcedure is the fully-qualified name of the AuthzAdminService's
	// UpdateRole RPC.
	AuthzAdminServiceUpdateRoleProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/UpdateRole"
	// AuthzAdminServiceDeleteRoleProcedure is the fully-qualified name of the AuthzAdminService's
	// DeleteRole RPC.
	AuthzAdminServiceDeleteRoleProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/DeleteRole"
	// AuthzAdminServiceAssignRoleProcedure is the fully-qualified name of the AuthzAdminService's
	// AssignRole RPC.
	AuthzAdminServiceAssignRoleProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/AssignRole"
	// AuthzAdminServiceUnassignRoleProcedure is the fully-qualified name of the AuthzAdminService's
	// UnassignRole RPC.
	AuthzAdminServiceUnassignRoleProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/UnassignRole"
	// AuthzAdminServiceListAccountRolesProcedure is the fully-qualified name of the AuthzAdminService's
	// ListAccountRoles RPC.
	AuthzAdminServiceListAccountRolesProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/ListAccountRoles"
	// AuthzAdminServiceListAuditEntriesProcedure is the fully-qualified name of the AuthzAdminService's
	// ListAuditEntries RPC.
	AuthzAdminServiceListAuditEntriesProcedure = "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/ListAuditEntries"
)

// AuthzServiceClient is a client for the __PROTO_PACKAGE__.authz.v1.AuthzService service.
type AuthzServiceClient interface {
	// ListMyGrants returns every permission the caller holds and the scope each reaches.
	//
	// Which account that is comes from the bearer token, so there is no id in the request and no way
	// to ask about somebody else's. A call with no verified caller is UNAUTHENTICATED rather than an
	// empty list: an account that may do nothing and an expired token draw the same screen and mean
	// opposite things.
	//
	// The client uses it to draw locks, not to enforce them. Every route is checked server-side
	// against the same table, so a client that never called this is refused identically.
	ListMyGrants(context.Context, *connect.Request[v1.ListMyGrantsRequest]) (*connect.Response[v1.ListMyGrantsResponse], error)
}

// NewAuthzServiceClient constructs a client for the __PROTO_PACKAGE__.authz.v1.AuthzService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthzServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthzServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authzServiceMethods := v1.File____PROTO_PACKAGE___authz_v1_authz_proto.Services().ByName("AuthzService").Methods()
	return &authzServiceClient{
		listMyGrants: connect.NewClient[v1.ListMyGrantsRequest, v1.ListMyGrantsResponse](
			httpClient,
			baseURL+AuthzServiceListMyGrantsProcedure,
			connect.WithSchema(authzServiceMethods.ByName("ListMyGrants")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authzServiceClient implements AuthzServiceClient.
type authzServiceClient struct {
	listMyGrants *connect.Client[v1.ListMyGrantsRequest, v1.ListMyGrantsResponse]
}

// ListMyGrants calls __PROTO_PACKAGE__.authz.v1.AuthzService.ListMyGrants.
func (c *authzServiceClient) ListMyGrants(ctx context.Context, req *connect.Request[v1.ListMyGrantsRequest]) (*connect.Response[v1.ListMyGrantsResponse], error) {
	return c.listMyGrants.CallUnary(ctx, req)
}

// AuthzServiceHandler is an implementation of the __PROTO_PACKAGE__.authz.v1.AuthzService service.
type AuthzServiceHandler interface {
	// ListMyGrants returns every permission the caller holds and the scope each reaches.
	//
	// Which account that is comes from the bearer token, so there is no id in the request and no way
	// to ask about somebody else's. A call with no verified caller is UNAUTHENTICATED rather than an
	// empty list: an account that may do nothing and an expired token draw the same screen and mean
	// opposite things.
	//
	// The client uses it to draw locks, not to enforce them. Every route is checked server-side
	// against the same table, so a client that never called this is refused identically.
	ListMyGrants(context.Context, *connect.Request[v1.ListMyGrantsRequest]) (*connect.Response[v1.ListMyGrantsResponse], error)
}

// NewAuthzServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthzServiceHandler(svc AuthzServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authzServiceMethods := v1.File____PROTO_PACKAGE___authz_v1_authz_proto.Services().ByName("AuthzService").Methods()
	authzServiceListMyGrantsHandler := connect.NewUnaryHandler(
		AuthzServiceListMyGrantsProcedure,
		svc.ListMyGrants,
		connect.WithSchema(authzServiceMethods.ByName("ListMyGrants")),
		connect.WithHandlerOptions(opts...),
	)
	return "/__PROTO_PACKAGE__.authz.v1.AuthzService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthzServiceListMyGrantsProcedure:
			authzServiceListMyGrantsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthzServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthzServiceHandler struct{}

func (UnimplementedAuthzServiceHandler) ListMyGrants(context.Context, *connect.Request[v1.ListMyGrantsRequest]) (*connect.Response[v1.ListMyGrantsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzService.ListMyGrants is not implemented"))
}

// AuthzAdminServiceClient is a client for the __PROTO_PACKAGE__.authz.v1.AuthzAdminService service.
type AuthzAdminServiceClient interface {
	// ListRoles returns every role with its counts.
	ListRoles(context.Context, *connect.Request[v1.ListRolesRequest]) (*connect.Response[v1.ListRolesResponse], error)
	// ListPermissions returns the whole catalogue, for the grid the screen renders.
	//
	// Unpaged: the count is bounded by how many permissions are compiled into a build, which is a
	// number a person maintains by hand.
	ListPermissions(context.Context, *connect.Request[v1.ListPermissionsRequest]) (*connect.Response[v1.ListPermissionsResponse], error)
	// GetRoleGrants returns one role's grants.
	GetRoleGrants(context.Context, *connect.Request[v1.GetRoleGrantsRequest]) (*connect.Response[v1.GetRoleGrantsResponse], error)
	// SaveRoleGrants replaces a role's entire grant set, in one act.
	//
	// The whole set rather than a diff, because that is what the screen has: an administrator flips
	// eight toggles and presses Save once, and sending eight calls would give them no way to change
	// their mind and would write eight audit entries for one decision. The server applies it in a
	// transaction, so a half-saved role is not a state anybody can observe.
	SaveRoleGrants(context.Context, *connect.Request[v1.SaveRoleGrantsRequest]) (*connect.Response[v1.SaveRoleGrantsResponse], error)
	// CreateRole adds a role, optionally copying another's grants — which is what "Clone role" is.
	CreateRole(context.Context, *connect.Request[v1.CreateRoleRequest]) (*connect.Response[v1.CreateRoleResponse], error)
	// UpdateRole renames a role or rewrites its description.
	//
	// A system role keeps its name: boot reconciles the shipped roles BY name, so renaming Admin
	// would have the next boot recreate "Admin" beside it. Its description may change.
	UpdateRole(context.Context, *connect.Request[v1.UpdateRoleRequest]) (*connect.Response[v1.UpdateRoleResponse], error)
	// DeleteRole removes a role and everything referring to it. Refused for a system role.
	DeleteRole(context.Context, *connect.Request[v1.DeleteRoleRequest]) (*connect.Response[v1.DeleteRoleResponse], error)
	// AssignRole gives an account a role, naming the account by email.
	//
	// Keyed by email because that is what an administrator has. The server resolves it and stores the
	// account's id, so a later change of address does not silently revoke anything. An address with
	// no account is NOT_FOUND rather than a grant waiting for one to appear.
	AssignRole(context.Context, *connect.Request[v1.AssignRoleRequest]) (*connect.Response[v1.AssignRoleResponse], error)
	// UnassignRole takes a role away.
	//
	// Effective on the next request rather than the next sign-in, because grants are resolved per
	// request and deliberately are not a claim in the token.
	UnassignRole(context.Context, *connect.Request[v1.UnassignRoleRequest]) (*connect.Response[v1.UnassignRoleResponse], error)
	// ListAccountRoles returns who holds which roles, for every account at once.
	//
	// Here rather than on the account service, and that is the module boundary showing through: the
	// account module owns people, this one owns what they may do, and an account message carrying
	// roles would be two modules holding the same fact. The users screen calls both and joins by id
	// — one extra round trip, in exchange for the two never disagreeing.
	ListAccountRoles(context.Context, *connect.Request[v1.ListAccountRolesRequest]) (*connect.Response[v1.ListAccountRolesResponse], error)
	// ListAuditEntries returns the most recent changes, newest first. Requires audit.view.
	ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error)
}

// NewAuthzAdminServiceClient constructs a client for the
// __PROTO_PACKAGE__.authz.v1.AuthzAdminService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuthzAdminServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuthzAdminServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	authzAdminServiceMethods := v1.File____PROTO_PACKAGE___authz_v1_authz_proto.Services().ByName("AuthzAdminService").Methods()
	return &authzAdminServiceClient{
		listRoles: connect.NewClient[v1.ListRolesRequest, v1.ListRolesResponse](
			httpClient,
			baseURL+AuthzAdminServiceListRolesProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("ListRoles")),
			connect.WithClientOptions(opts...),
		),
		listPermissions: connect.NewClient[v1.ListPermissionsRequest, v1.ListPermissionsResponse](
			httpClient,
			baseURL+AuthzAdminServiceListPermissionsProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("ListPermissions")),
			connect.WithClientOptions(opts...),
		),
		getRoleGrants: connect.NewClient[v1.GetRoleGrantsRequest, v1.GetRoleGrantsResponse](
			httpClient,
			baseURL+AuthzAdminServiceGetRoleGrantsProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("GetRoleGrants")),
			connect.WithClientOptions(opts...),
		),
		saveRoleGrants: connect.NewClient[v1.SaveRoleGrantsRequest, v1.SaveRoleGrantsResponse](
			httpClient,
			baseURL+AuthzAdminServiceSaveRoleGrantsProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("SaveRoleGrants")),
			connect.WithClientOptions(opts...),
		),
		createRole: connect.NewClient[v1.CreateRoleRequest, v1.CreateRoleResponse](
			httpClient,
			baseURL+AuthzAdminServiceCreateRoleProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("CreateRole")),
			connect.WithClientOptions(opts...),
		),
		updateRole: connect.NewClient[v1.UpdateRoleRequest, v1.UpdateRoleResponse](
			httpClient,
			baseURL+AuthzAdminServiceUpdateRoleProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("UpdateRole")),
			connect.WithClientOptions(opts...),
		),
		deleteRole: connect.NewClient[v1.DeleteRoleRequest, v1.DeleteRoleResponse](
			httpClient,
			baseURL+AuthzAdminServiceDeleteRoleProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("DeleteRole")),
			connect.WithClientOptions(opts...),
		),
		assignRole: connect.NewClient[v1.AssignRoleRequest, v1.AssignRoleResponse](
			httpClient,
			baseURL+AuthzAdminServiceAssignRoleProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("AssignRole")),
			connect.WithClientOptions(opts...),
		),
		unassignRole: connect.NewClient[v1.UnassignRoleRequest, v1.UnassignRoleResponse](
			httpClient,
			baseURL+AuthzAdminServiceUnassignRoleProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("UnassignRole")),
			connect.WithClientOptions(opts...),
		),
		listAccountRoles: connect.NewClient[v1.ListAccountRolesRequest, v1.ListAccountRolesResponse](
			httpClient,
			baseURL+AuthzAdminServiceListAccountRolesProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("ListAccountRoles")),
			connect.WithClientOptions(opts...),
		),
		listAuditEntries: connect.NewClient[v1.ListAuditEntriesRequest, v1.ListAuditEntriesResponse](
			httpClient,
			baseURL+AuthzAdminServiceListAuditEntriesProcedure,
			connect.WithSchema(authzAdminServiceMethods.ByName("ListAuditEntries")),
			connect.WithClientOptions(opts...),
		),
	}
}

// authzAdminServiceClient implements AuthzAdminServiceClient.
type authzAdminServiceClient struct {
	listRoles        *connect.Client[v1.ListRolesRequest, v1.ListRolesResponse]
	listPermissions  *connect.Client[v1.ListPermissionsRequest, v1.ListPermissionsResponse]
	getRoleGrants    *connect.Client[v1.GetRoleGrantsRequest, v1.GetRoleGrantsResponse]
	saveRoleGrants   *connect.Client[v1.SaveRoleGrantsRequest, v1.SaveRoleGrantsResponse]
	createRole       *connect.Client[v1.CreateRoleRequest, v1.CreateRoleResponse]
	updateRole       *connect.Client[v1.UpdateRoleRequest, v1.UpdateRoleResponse]
	deleteRole       *connect.Client[v1.DeleteRoleRequest, v1.DeleteRoleResponse]
	assignRole       *connect.Client[v1.AssignRoleRequest, v1.AssignRoleResponse]
	unassignRole     *connect.Client[v1.UnassignRoleRequest, v1.UnassignRoleResponse]
	listAccountRoles *connect.Client[v1.ListAccountRolesRequest, v1.ListAccountRolesResponse]
	listAuditEntries *connect.Client[v1.ListAuditEntriesRequest, v1.ListAuditEntriesResponse]
}

// ListRoles calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListRoles.
func (c *authzAdminServiceClient) ListRoles(ctx context.Context, req *connect.Request[v1.ListRolesRequest]) (*connect.Response[v1.ListRolesResponse], error) {
	return c.listRoles.CallUnary(ctx, req)
}

// ListPermissions calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListPermissions.
func (c *authzAdminServiceClient) ListPermissions(ctx context.Context, req *connect.Request[v1.ListPermissionsRequest]) (*connect.Response[v1.ListPermissionsResponse], error) {
	return c.listPermissions.CallUnary(ctx, req)
}

// GetRoleGrants calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.GetRoleGrants.
func (c *authzAdminServiceClient) GetRoleGrants(ctx context.Context, req *connect.Request[v1.GetRoleGrantsRequest]) (*connect.Response[v1.GetRoleGrantsResponse], error) {
	return c.getRoleGrants.CallUnary(ctx, req)
}

// SaveRoleGrants calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.SaveRoleGrants.
func (c *authzAdminServiceClient) SaveRoleGrants(ctx context.Context, req *connect.Request[v1.SaveRoleGrantsRequest]) (*connect.Response[v1.SaveRoleGrantsResponse], error) {
	return c.saveRoleGrants.CallUnary(ctx, req)
}

// CreateRole calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.CreateRole.
func (c *authzAdminServiceClient) CreateRole(ctx context.Context, req *connect.Request[v1.CreateRoleRequest]) (*connect.Response[v1.CreateRoleResponse], error) {
	return c.createRole.CallUnary(ctx, req)
}

// UpdateRole calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.UpdateRole.
func (c *authzAdminServiceClient) UpdateRole(ctx context.Context, req *connect.Request[v1.UpdateRoleRequest]) (*connect.Response[v1.UpdateRoleResponse], error) {
	return c.updateRole.CallUnary(ctx, req)
}

// DeleteRole calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.DeleteRole.
func (c *authzAdminServiceClient) DeleteRole(ctx context.Context, req *connect.Request[v1.DeleteRoleRequest]) (*connect.Response[v1.DeleteRoleResponse], error) {
	return c.deleteRole.CallUnary(ctx, req)
}

// AssignRole calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.AssignRole.
func (c *authzAdminServiceClient) AssignRole(ctx context.Context, req *connect.Request[v1.AssignRoleRequest]) (*connect.Response[v1.AssignRoleResponse], error) {
	return c.assignRole.CallUnary(ctx, req)
}

// UnassignRole calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.UnassignRole.
func (c *authzAdminServiceClient) UnassignRole(ctx context.Context, req *connect.Request[v1.UnassignRoleRequest]) (*connect.Response[v1.UnassignRoleResponse], error) {
	return c.unassignRole.CallUnary(ctx, req)
}

// ListAccountRoles calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListAccountRoles.
func (c *authzAdminServiceClient) ListAccountRoles(ctx context.Context, req *connect.Request[v1.ListAccountRolesRequest]) (*connect.Response[v1.ListAccountRolesResponse], error) {
	return c.listAccountRoles.CallUnary(ctx, req)
}

// ListAuditEntries calls __PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListAuditEntries.
func (c *authzAdminServiceClient) ListAuditEntries(ctx context.Context, req *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error) {
	return c.listAuditEntries.CallUnary(ctx, req)
}

// AuthzAdminServiceHandler is an implementation of the __PROTO_PACKAGE__.authz.v1.AuthzAdminService
// service.
type AuthzAdminServiceHandler interface {
	// ListRoles returns every role with its counts.
	ListRoles(context.Context, *connect.Request[v1.ListRolesRequest]) (*connect.Response[v1.ListRolesResponse], error)
	// ListPermissions returns the whole catalogue, for the grid the screen renders.
	//
	// Unpaged: the count is bounded by how many permissions are compiled into a build, which is a
	// number a person maintains by hand.
	ListPermissions(context.Context, *connect.Request[v1.ListPermissionsRequest]) (*connect.Response[v1.ListPermissionsResponse], error)
	// GetRoleGrants returns one role's grants.
	GetRoleGrants(context.Context, *connect.Request[v1.GetRoleGrantsRequest]) (*connect.Response[v1.GetRoleGrantsResponse], error)
	// SaveRoleGrants replaces a role's entire grant set, in one act.
	//
	// The whole set rather than a diff, because that is what the screen has: an administrator flips
	// eight toggles and presses Save once, and sending eight calls would give them no way to change
	// their mind and would write eight audit entries for one decision. The server applies it in a
	// transaction, so a half-saved role is not a state anybody can observe.
	SaveRoleGrants(context.Context, *connect.Request[v1.SaveRoleGrantsRequest]) (*connect.Response[v1.SaveRoleGrantsResponse], error)
	// CreateRole adds a role, optionally copying another's grants — which is what "Clone role" is.
	CreateRole(context.Context, *connect.Request[v1.CreateRoleRequest]) (*connect.Response[v1.CreateRoleResponse], error)
	// UpdateRole renames a role or rewrites its description.
	//
	// A system role keeps its name: boot reconciles the shipped roles BY name, so renaming Admin
	// would have the next boot recreate "Admin" beside it. Its description may change.
	UpdateRole(context.Context, *connect.Request[v1.UpdateRoleRequest]) (*connect.Response[v1.UpdateRoleResponse], error)
	// DeleteRole removes a role and everything referring to it. Refused for a system role.
	DeleteRole(context.Context, *connect.Request[v1.DeleteRoleRequest]) (*connect.Response[v1.DeleteRoleResponse], error)
	// AssignRole gives an account a role, naming the account by email.
	//
	// Keyed by email because that is what an administrator has. The server resolves it and stores the
	// account's id, so a later change of address does not silently revoke anything. An address with
	// no account is NOT_FOUND rather than a grant waiting for one to appear.
	AssignRole(context.Context, *connect.Request[v1.AssignRoleRequest]) (*connect.Response[v1.AssignRoleResponse], error)
	// UnassignRole takes a role away.
	//
	// Effective on the next request rather than the next sign-in, because grants are resolved per
	// request and deliberately are not a claim in the token.
	UnassignRole(context.Context, *connect.Request[v1.UnassignRoleRequest]) (*connect.Response[v1.UnassignRoleResponse], error)
	// ListAccountRoles returns who holds which roles, for every account at once.
	//
	// Here rather than on the account service, and that is the module boundary showing through: the
	// account module owns people, this one owns what they may do, and an account message carrying
	// roles would be two modules holding the same fact. The users screen calls both and joins by id
	// — one extra round trip, in exchange for the two never disagreeing.
	ListAccountRoles(context.Context, *connect.Request[v1.ListAccountRolesRequest]) (*connect.Response[v1.ListAccountRolesResponse], error)
	// ListAuditEntries returns the most recent changes, newest first. Requires audit.view.
	ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error)
}

// NewAuthzAdminServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuthzAdminServiceHandler(svc AuthzAdminServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	authzAdminServiceMethods := v1.File____PROTO_PACKAGE___authz_v1_authz_proto.Services().ByName("AuthzAdminService").Methods()
	authzAdminServiceListRolesHandler := connect.NewUnaryHandler(
		AuthzAdminServiceListRolesProcedure,
		svc.ListRoles,
		connect.WithSchema(authzAdminServiceMethods.ByName("ListRoles")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceListPermissionsHandler := connect.NewUnaryHandler(
		AuthzAdminServiceListPermissionsProcedure,
		svc.ListPermissions,
		connect.WithSchema(authzAdminServiceMethods.ByName("ListPermissions")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceGetRoleGrantsHandler := connect.NewUnaryHandler(
		AuthzAdminServiceGetRoleGrantsProcedure,
		svc.GetRoleGrants,
		connect.WithSchema(authzAdminServiceMethods.ByName("GetRoleGrants")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceSaveRoleGrantsHandler := connect.NewUnaryHandler(
		AuthzAdminServiceSaveRoleGrantsProcedure,
		svc.SaveRoleGrants,
		connect.WithSchema(authzAdminServiceMethods.ByName("SaveRoleGrants")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceCreateRoleHandler := connect.NewUnaryHandler(
		AuthzAdminServiceCreateRoleProcedure,
		svc.CreateRole,
		connect.WithSchema(authzAdminServiceMethods.ByName("CreateRole")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceUpdateRoleHandler := connect.NewUnaryHandler(
		AuthzAdminServiceUpdateRoleProcedure,
		svc.UpdateRole,
		connect.WithSchema(authzAdminServiceMethods.ByName("UpdateRole")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceDeleteRoleHandler := connect.NewUnaryHandler(
		AuthzAdminServiceDeleteRoleProcedure,
		svc.DeleteRole,
		connect.WithSchema(authzAdminServiceMethods.ByName("DeleteRole")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceAssignRoleHandler := connect.NewUnaryHandler(
		AuthzAdminServiceAssignRoleProcedure,
		svc.AssignRole,
		connect.WithSchema(authzAdminServiceMethods.ByName("AssignRole")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceUnassignRoleHandler := connect.NewUnaryHandler(
		AuthzAdminServiceUnassignRoleProcedure,
		svc.UnassignRole,
		connect.WithSchema(authzAdminServiceMethods.ByName("UnassignRole")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceListAccountRolesHandler := connect.NewUnaryHandler(
		AuthzAdminServiceListAccountRolesProcedure,
		svc.ListAccountRoles,
		connect.WithSchema(authzAdminServiceMethods.ByName("ListAccountRoles")),
		connect.WithHandlerOptions(opts...),
	)
	authzAdminServiceListAuditEntriesHandler := connect.NewUnaryHandler(
		AuthzAdminServiceListAuditEntriesProcedure,
		svc.ListAuditEntries,
		connect.WithSchema(authzAdminServiceMethods.ByName("ListAuditEntries")),
		connect.WithHandlerOptions(opts...),
	)
	return "/__PROTO_PACKAGE__.authz.v1.AuthzAdminService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuthzAdminServiceListRolesProcedure:
			authzAdminServiceListRolesHandler.ServeHTTP(w, r)
		case AuthzAdminServiceListPermissionsProcedure:
			authzAdminServiceListPermissionsHandler.ServeHTTP(w, r)
		case AuthzAdminServiceGetRoleGrantsProcedure:
			authzAdminServiceGetRoleGrantsHandler.ServeHTTP(w, r)
		case AuthzAdminServiceSaveRoleGrantsProcedure:
			authzAdminServiceSaveRoleGrantsHandler.ServeHTTP(w, r)
		case AuthzAdminServiceCreateRoleProcedure:
			authzAdminServiceCreateRoleHandler.ServeHTTP(w, r)
		case AuthzAdminServiceUpdateRoleProcedure:
			authzAdminServiceUpdateRoleHandler.ServeHTTP(w, r)
		case AuthzAdminServiceDeleteRoleProcedure:
			authzAdminServiceDeleteRoleHandler.ServeHTTP(w, r)
		case AuthzAdminServiceAssignRoleProcedure:
			authzAdminServiceAssignRoleHandler.ServeHTTP(w, r)
		case AuthzAdminServiceUnassignRoleProcedure:
			authzAdminServiceUnassignRoleHandler.ServeHTTP(w, r)
		case AuthzAdminServiceListAccountRolesProcedure:
			authzAdminServiceListAccountRolesHandler.ServeHTTP(w, r)
		case AuthzAdminServiceListAuditEntriesProcedure:
			authzAdminServiceListAuditEntriesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuthzAdminServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuthzAdminServiceHandler struct{}

func (UnimplementedAuthzAdminServiceHandler) ListRoles(context.Context, *connect.Request[v1.ListRolesRequest]) (*connect.Response[v1.ListRolesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListRoles is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) ListPermissions(context.Context, *connect.Request[v1.ListPermissionsRequest]) (*connect.Response[v1.ListPermissionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListPermissions is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) GetRoleGrants(context.Context, *connect.Request[v1.GetRoleGrantsRequest]) (*connect.Response[v1.GetRoleGrantsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.GetRoleGrants is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) SaveRoleGrants(context.Context, *connect.Request[v1.SaveRoleGrantsRequest]) (*connect.Response[v1.SaveRoleGrantsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.SaveRoleGrants is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) CreateRole(context.Context, *connect.Request[v1.CreateRoleRequest]) (*connect.Response[v1.CreateRoleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.CreateRole is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) UpdateRole(context.Context, *connect.Request[v1.UpdateRoleRequest]) (*connect.Response[v1.UpdateRoleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.UpdateRole is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) DeleteRole(context.Context, *connect.Request[v1.DeleteRoleRequest]) (*connect.Response[v1.DeleteRoleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.DeleteRole is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) AssignRole(context.Context, *connect.Request[v1.AssignRoleRequest]) (*connect.Response[v1.AssignRoleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.AssignRole is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) UnassignRole(context.Context, *connect.Request[v1.UnassignRoleRequest]) (*connect.Response[v1.UnassignRoleResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.UnassignRole is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) ListAccountRoles(context.Context, *connect.Request[v1.ListAccountRolesRequest]) (*connect.Response[v1.ListAccountRolesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListAccountRoles is not implemented"))
}

func (UnimplementedAuthzAdminServiceHandler) ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("__PROTO_PACKAGE__.authz.v1.AuthzAdminService.ListAuditEntries is not implemented"))
}
